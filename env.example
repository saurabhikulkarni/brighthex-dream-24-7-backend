# Server Configuration
PORT=3000
NODE_ENV=development

# MSG91 Configuration
# Get from: https://control.msg91.com/
MSG91_AUTH_KEY=460639ALxMlUU8Q688c88b6P1
MSG91_TEMPLATE_ID=688b0b58d6fc05287e15c132
MSG91_SENDER_ID=DREAM24

# Razorpay Configuration
# Get from: https://dashboard.razorpay.com/#/app/keys
RAZORPAY_KEY_ID=rzp_test_RqEBl9COpBTyyz
RAZORPAY_KEY_SECRET=ZYMi5yOCKq7HpZ2k6jsXrMtR
# Webhook secret is optional - if not set, webhooks will work without signature verification (less secure)
# Get webhook secret from: https://dashboard.razorpay.com/#/app/webhooks
RAZORPAY_WEBHOOK_SECRET=your_webhook_secret_optional

# CORS Configuration
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# Hygraph CMS Configuration (Primary Data Storage)
# Get from: https://hygraph.com/
# Use your Content API endpoint
# Example: https://ap-south-1.cdn.hygraph.com/content/YOUR_PROJECT_ID/YOUR_ENV
HYGRAPH_ENDPOINT=https://ap-south-1.cdn.hygraph.com/content/cmj85rtgv038n07uo8egj5fkb/master
# HYGRAPH_TOKEN is required for mutations (create/update/delete operations)
# Get a permanent auth token from Hygraph Dashboard → Settings → API Access → Permanent Auth Tokens
# Ensure the token has mutations permissions enabled
HYGRAPH_TOKEN=your_hygraph_permanent_auth_token

# Shiprocket Configuration
# Get from: https://app.shiprocket.in/settings/api
# OPTION 1: Use direct API token (RECOMMENDED)
# Token is valid for 240 hours (10 days)
SHIPROCKET_API_TOKEN=your_shiprocket_api_token_here
# OPTION 2: Use API User credentials (fallback)
# IMPORTANT: These must be API User credentials, NOT your main Shiprocket account!
# Steps to create API User:
# 1. Go to Shiprocket Panel → Settings → API
# 2. Click "Create an API User"
# 3. Enter a Valid Email ID (MUST be different from your registered email)
# 4. Input an appropriate password
# 5. Use these credentials below (NOT your main account credentials)
SHIPROCKET_EMAIL=brigthtestShipTest@gmail.com
# IMPORTANT: Quote the password if it contains special characters like #, !, &, etc.
# The # character starts a comment in .env files, so unquoted passwords with # will be truncated
SHIPROCKET_PASSWORD="Xm1ULRgo*I9rC5NvZUW!K#^^Bo&Tx*C8"

# JWT Secret (MUST BE SAME AS FANTASY APP)
# This secret is used to sign and verify JWT tokens
# CRITICAL: Use the same secret in both shopping and fantasy apps for seamless authentication
SECRET_TOKEN=your-secret-key-here

# Fantasy App Integration (Optional)
# Used to sync user data between shopping app (Hygraph) and fantasy app (Firebase)
FANTASY_API_URL=http://localhost:8000
FANTASY_API_KEY=your_fantasy_api_key_here